# MVP Tech Debt

- Runtime image assets currently keep a placeholder-render fallback in [reverse_detective/renderer.py](/d:/lls/reverse_detective/renderer.py) because the current `crs`/Codex-compatible provider used by `config.toml` exposes live scene text generation but does not expose a working OpenAI-compatible image generation route. Real image rendering works only when the configured provider supports either the Responses `image_generation` tool or the Images API.
- The checked-in runtime art in [assets/img/SOURCES.md](/d:/lls/assets/img/SOURCES.md) still relies on temporary Kenney placeholder packs. A curated swap to the selected non-cartoon noir/VN-oriented libraries has been specified, but the actual image files and [assets/img/catalog.json](/d:/lls/assets/img/catalog.json) mappings have not yet been fully replaced.
- Scene JSON now supports per-option `resolution_mode` in [reverse_detective/models.py](/d:/lls/reverse_detective/models.py), [reverse_detective/scene_loader.py](/d:/lls/reverse_detective/scene_loader.py), and [reverse_detective/ai_client.py](/d:/lls/reverse_detective/ai_client.py) so the client can distinguish predictable local-rule actions from rare immediate AI adjudication triggers. The checked-in gameplay README schema has not been synchronized in this change because [README.md](/d:/lls/README.md) already has local user edits in the worktree and should be merged carefully instead of overwritten.
- Initial scene generation now supports background prefetch plus local disk cache in [reverse_detective/ai_client.py](/d:/lls/reverse_detective/ai_client.py) and [reverse_detective/app.py](/d:/lls/reverse_detective/app.py) so story selection can warm the first scene before the player enters gameplay. This startup acceleration path is not yet documented in [README.md](/d:/lls/README.md) because the file already has local user edits and should be synchronized deliberately.
